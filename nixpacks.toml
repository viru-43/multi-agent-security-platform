[phases.setup]
nixPkgs = ["nodejs_20", "python313", "python313Packages.pip"]

[phases.install]
cmds = [
    "pip install -r requirements.txt"
]

[start]
cmd = "uvicorn orchestrator.main:app --host 0.0.0.0 --port $PORT"